import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Represents an actor that displays a character image.
 */
public class CharacterDisplay extends Actor {
    /**
     * Creates an instance of an image
     */
    public CharacterDisplay(GreenfootImage image) {
        setImage(image);
    }
}
